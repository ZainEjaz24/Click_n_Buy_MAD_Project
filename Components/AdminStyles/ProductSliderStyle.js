import { StyleSheet } from "react-native";

const productSliderStyle = StyleSheet.create({
  categoryimg: {
    width: 60,
    height: 60,
    borderRadius: 25,
  },
  categoryPressable: {
    justifyContent: "center",
    alignItems: "center",
    margin: 12,
  },
});

export default productSliderStyle;
